-- =====================================================================
-- إرفاق أصناف فعلية بطلب التمويل (من السلة / يدوي / تسعيرة مورّد)
-- ليتضح للأدمن ما المطلوب شراؤه بدل وصف نصّي غير دقيق.
-- =====================================================================
ALTER TABLE public.financing_requests
  ADD COLUMN IF NOT EXISTS items JSONB NOT NULL DEFAULT '[]'::jsonb;
