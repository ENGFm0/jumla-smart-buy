-- =====================================================================
-- تسعير حسب الكمية (شرائح): المورّد يضيف عروضاً على العدد.
-- price_tiers = مصفوفة [{ "min": <أقل كمية>, "price": <السعر> }] إضافة لسعر القطعة (price).
-- =====================================================================
ALTER TABLE public.offers ADD COLUMN IF NOT EXISTS price_tiers JSONB;
